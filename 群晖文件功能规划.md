# 群晖文件功能规划

日期：2026-09-12。状态：第一期已实现，验收结果以 `验证记录.md` 为准；第二期仍为规划；第三期中的原生视频播放已在 0.3.0 实现，后台下载、断点续传及转码仍未实现。

## 目标与入口

用户希望读取各种群晖文件，先完成目录浏览与下载，再扩展预览能力。首版交付目标：在 iPhone 进入一个有权限的共享文件夹，找到任意类型文件，下载完成，保存到系统「文件」，断网后仍能打开已下载副本。

沿用底部「群晖」入口，在页面顶部增加「照片 / 文件」切换。文件页默认显示该账号能访问的共享文件夹，并提供「下载任务」「已下载」入口。账号及服务器沿用设置中已保存的连接；照片与文件分别显示连接状态。

流程：群晖 → 文件 → 共享文件夹 → 子目录 → 文件详情 → 下载 → 已下载 → 保存到「文件」或用其他 App 打开。

## 分期范围

| 阶段 | 功能 | 完成标准 |
| --- | --- | --- |
| 第一期：目录与下载 | 共享文件夹、多级目录、返回上级、路径导航、目录分页、下拉刷新；按名称／时间／大小排序；文件详情；单个文件加入下载队列、进度、取消、失败重试；已下载列表、导出到系统「文件」 | 各种扩展名均可浏览和下载；下载文件与测试原件一致；无权限、断网、会话失效、空间不足均显示对应原因 |
| 第二期：预览与查找 | 图片、PDF、TXT、系统支持的 Office 文档预览；文件名搜索、常用目录、最近访问；批量文件下载、文件夹打包下载 | 能直接阅读受支持的文件，搜索范围明确，批量任务与原件对应 |
| 第三期：大文件与影音 | 后台下载、断点续传、音视频播放、播放进度 | 在真实 NAS 验证后台任务、会话续期、断点请求及对应编码后逐项开放 |

第一期以读取与下载为范围；上传、远端删除、改名和移动另行规划。下载失败重试先从头开始，暂停续传归入第三期。目录可逐层浏览，整个文件夹下载归入第二期。第一期文件类型不设扩展名白名单，但预览能力取决于后续阅读器支持。

## 界面行为

- 目录列表显示文件夹／文件图标、名称、文件大小、修改时间；文件夹优先。滚动分页时沿用同一排序，避免只对已加载的一页排序。
- 点击文件夹进入下一层；点击文件显示详情与下载按钮。目录为空与读取失败显示不同页面。
- 下载任务显示排队、下载中、已完成、失败、已取消；未知总大小时显示已接收大小，不显示虚假的百分比。首版并发上限建议为 2。
- 已下载文件保存在 App 本地持久目录，重启或断网后仍可见。用户可保存副本到系统「文件」，也可通过系统分享面板选择其他 App。
- 同名文件使用独立本地标识，展示名称保持原名。删除本地下载只删除本机副本；下载完成前的临时内容不会出现在「已下载」。

## 接入方案

使用群晖 File Station 官方 API。官方文档定义了共享目录、子目录、文件详情和下载接口，并要求 File Station 会话；接口路径及版本通过 API 发现确定。[群晖 File Station API Guide](https://global.download.synology.com/download/Document/Software/DeveloperGuide/Package/FileStation/All/enu/Synology_File_Station_API_Guide.pdf)

| 用途 | 接口 |
| --- | --- |
| 发现能力 | `SYNO.API.Info` |
| 文件服务认证 | `SYNO.API.Auth`，`session=FileStation` |
| 共享文件夹／目录／详情 | `SYNO.FileStation.List` 的 `list_share`、`list`、`getinfo` |
| 单文件下载 | `SYNO.FileStation.Download` 的 `download` |

现有 `SynologyClient` 的登录会话固定为 `SynologyPhotos`，并强制要求 Photos 接口存在。因此需要复用连接配置和请求基础代码，为 File Station 建立独立会话，不能直接把 Photos 登录成功当作文件权限已通过。File Station 不可用时保留照片功能，照片服务不可用时也不应阻止文件服务连接。

账号需要 File Station 使用权限及对应共享目录读取权限。首次实现时再检测这台 NAS 实际暴露的接口、路径和权限；实现时已通过不带凭据的 API 发现确认真实 NAS 提供版本 2 的列表与下载接口；账号权限仍需真机登录验证。

## 代码拆分

- `SynologyClient`（已实现）：复用现有 API 发现、HTTPS 请求、响应解析与会话传输，通过服务参数创建独立的 Photos／File Station 客户端实例。沿用 Cookie／token 兼容修复，认证错误按接口解释。
- `FileStation.swift`（已实现）：文件模型、共享目录、分页列表、详情、下载请求。文件接口使用共享文件夹路径，不使用 Photos 的照片 ID 或文件夹 ID。
- `NASFileBrowserStore`：当前路径、导航历史、排序、分页及错误状态。切换目录或账号后取消旧请求，防止旧目录结果覆盖新页面。
- `NASDownloadManager`：下载委托接收进度，文件直接写磁盘；持久记录任务和完成文件，校验状态与可用的长度信息，取消或失败时清理临时文件。通用文件下载不能沿用现有图片格式校验；合法 JSON、ZIP 等文件必须原样保存。
- `NASFilesView`、`NASDownloadsView`：目录、文件详情、下载任务、已下载文件和导出。下载记录按 NAS 与账号隔离，重新登录后不会混入其他账号的目录状态。
- `FilePreview`（第二期）：下载到独立预览缓存后交给系统 Quick Look；先检查预览支持，无法预览时保留导出能力。Quick Look 支持常见文档及媒体，但支持范围可能随系统变化，需用样本验证。[Apple Quick Look](https://developer.apple.com/documentation/quicklook/)

持久下载与可清理的预览缓存分开管理。账号密码继续保存在系统钥匙串；会话不放进分享链接或日志。断点和流媒体需要单独验证认证及服务端支持，不根据文件后缀承诺全部视频可以播放。

## 开发顺序与验收

1. 接通 File Station 独立认证及共享目录列表；验证真实 NAS 的应用权限。回归现有 Photos 登录和照片读取。
2. 完成目录导航与分页；测试空目录、超过一页的目录、中文／空格／特殊字符文件名、无权限目录及切换目录时的请求取消。
3. 完成下载队列与本地保存；使用已知内容的 PDF、TXT、JSON、ZIP、图片、视频测试文件校验下载结果。加入断网、重新登录、同名文件、空间不足及取消后的清理检查。
4. 在模拟器验收完整流程，再安装到 iPhone：从用户选择的 NAS 目录下载一个文件，保存到系统「文件」，断网打开副本；同时确认群晖照片仍可读取。

第一期完成以实际 iPhone 浏览和下载成功为准，不以接口请求返回成功或编译通过替代。
